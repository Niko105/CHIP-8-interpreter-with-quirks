# CHIP-8-interpreter-with-quirks
CHIP-8 interpreter capable of supporting the CHIP8 and SCHIP with some configuration.

Written entirely in Clojure, trying to adhere to the paradigm as much as possible. Runs entirely on the terminal, rendering and input.

### THIS IS NOT A COMPILER, IT ONLY EXECUTES THE CODE
